import Button from './Button'

const DevOverlay = () => {
	return (
		<div className='fixed bottom-5 right-5'>
			<Button
				text="Test"
				onClick={() => {}}
			/>
		</div>
	)
}

export default DevOverlay